/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Wall2 : MonoBehaviour
{
    public int width;
    public int height;

    GameObject DoBrick(Vector3 pos, string name)
    {
        GameObject brick = GameObject.CreatePrimitive(PrimitiveType.Cube);
        brick.transform.position = pos;
        brick.transform.localScale = new Vector3(1, 0.5f, 0.5f);
        brick.name = name;
        return brick;
    }

    // DoWall method receives two parameters: width and height, and creates a wall of bricks with those dimensions.
    //Each 

    // Start is called before the first frame update
    void Start()
    {
        DoWall(width, height);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}*/
